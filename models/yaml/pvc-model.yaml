apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qwen3-9b
  namespace: llm-model
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  volumeMode: Filesystem
